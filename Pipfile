[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
lightspark = "*"
flask = "*"
requests = "*"
# NOTE: This file needs to exist in this directory for docker to build
# to generate an updated version, run `pipenv run python setup.py sdist --format=zip`
# in the `uma-python-sdk` directory, then copy the resulting zip file here.
uma-sdk = {path = "./uma-sdk-1.0.0a1.zip"}

[dev-packages]
black = "*"
isort = "*"
pyre-check = "*"
pytest = "*"
pylint = "*"

[requires]
python_version = "3.11"
